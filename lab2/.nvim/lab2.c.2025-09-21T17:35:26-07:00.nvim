#include <stdio.h>
#define _POSIX_C_SOURCE 200809L
#include <stdlib.h>

int main() {
  printf("Enter programs to run.\n>");

  char *line = NULL;
  size_t len = 0;
  token = getline(&line, &len, stdin);

  free(buff);
}
