#define _GNU_SOURCE
#include <stdio.h>
#include <stdlib.h>

int main() {
  printf("Enter programs to run.\n>");

  char *line = NULL;
  size_t len = 0;
  token = getline(&line, &len, stdin);

  free(buff);
}
